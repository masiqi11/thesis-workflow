# Optional dependencies for local helper tools
# Keep minimal for GitHub-friendly distribution.

# Runtime: tools/*.py have zero runtime dependencies (stdlib only).

# Dev / test (install with: pip install -r requirements.txt --extra)
# pytest>=7.0      # required to run tests/ — uncomment locally
